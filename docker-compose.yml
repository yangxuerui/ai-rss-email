services:
  ai-rss-email:
    image: ai-rss-email:latest
    build: .
    container_name: ai-rss-email
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./config.yaml:/app/config.yaml:ro
